'use client';

/**
 * Shim for embedded studio-lib paths that import `@/lib/hooks/useRequestCache`.
 * Implementation lives under `src/studio-lib/lib/hooks/`.
 */
export { useRequestCache, globalRequestCache } from '../../studio-lib/lib/hooks/useRequestCache';
